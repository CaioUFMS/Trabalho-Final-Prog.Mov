<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lojaterere">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Loja do Tereré"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LojaTerere">
        <activity android:name=".FinalizarCompra"></activity>
        <activity android:name=".EditarProdutoCarrinho" />
        <activity android:name=".VisualizarCarrinho" />
        <activity android:name=".EdicaoProduto" />
        <activity android:name=".RealizarVenda" />
        <activity android:name=".ManejoEstoque" />
        <activity android:name=".CadastroProduto" />
        <activity android:name=".SelecaoFuncao" />
        <activity android:name=".Cadastro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>